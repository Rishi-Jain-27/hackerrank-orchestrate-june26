"""Evaluation package: scores the system on dataset/sample_claims.csv.

Package marker so the metric functions in `evaluation.main` are importable and
unit-testable. The runnable entry point is `evaluation/main.py` (AGENTS.md §6.1).
"""
